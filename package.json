{
  "name": "lme-trade-request",
  "version": "1.0.0",
  "description": "LME Trade Request Generator",
  "main": "main.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint -c .eslintrc.cjs --ignore-pattern '*.min.js' --ignore-pattern 'tailwind.min.css' '**/*.js'",
    "format": "prettier --write '**/*.{js,html,json,md}'"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "jest": "^29.6.1",
    "jest-environment-jsdom": "^30.0.0-beta.3",
    "prettier": "^3.5.3"
  }
}
